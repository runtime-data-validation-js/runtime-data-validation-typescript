{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npx tsc",
    "watch": "npx tsc -w",
    "test": "mocha ./lib/numbers.js ./lib/strings.js",
    "monitor": "nodemon --watch lib --watch ../dist --exec 'npm run test'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chai": "^4.3.6",
    "mocha": "^9.2.1",
    "runtime-data-validation": "file:.."
  },
  "devDependencies": {
    "@types/mocha": "^9.1.0",
    "@types/node": "^17.0.18",
    "nodemon": "^2.0.15"
  }
}
